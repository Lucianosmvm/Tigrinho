<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, viewport-fit=cover">
<title>Tigrinho da Sorte</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Cinzel+Decorative:wght@700;900&family=Poppins:wght@400;500;600;700;800&family=JetBrains+Mono:wght@500;700&display=swap" rel="stylesheet">
<style>
  :root{
    --bg-deep:#0a1810;
    --bg-deep-2:#0f2117;
    --bg-panel:#142b1e;
    --bg-panel-2:#1b3826;
    --gold:#e8b33d;
    --gold-bright:#ffd874;
    --gold-dim:#a87f2c;
    --red:#b3262a;
    --red-bright:#e63d3d;
    --jade:#3f7a5d;
    --cream:#f3e7c9;
    --dim:#9fb6a6;
    --shadow-deep: 0 20px 60px rgba(0,0,0,0.6);
  }

  *{box-sizing:border-box; -webkit-tap-highlight-color:transparent;}
  html,body{margin:0;padding:0;}
  button{touch-action:manipulation;}

  body{
    min-height:100vh;
    min-height:100dvh;
    background:
      radial-gradient(ellipse at 50% -10%, rgba(63,122,93,0.35), transparent 60%),
      radial-gradient(ellipse at 10% 110%, rgba(179,38,42,0.18), transparent 55%),
      linear-gradient(180deg, var(--bg-deep) 0%, var(--bg-deep-2) 100%);
    font-family:'Poppins', sans-serif;
    color:var(--cream);
    display:flex;
    flex-direction:column;
    align-items:center;
    padding:28px 14px calc(60px + env(safe-area-inset-bottom));
    padding-top:calc(28px + env(safe-area-inset-top));
    overflow-x:hidden;
    width:100%;
    position:relative;
  }

  /* faint bamboo / leaf texture using repeating gradients */
  body::before{
    content:"";
    position:fixed;
    inset:0;
    background-image:
      repeating-linear-gradient(115deg, rgba(63,122,93,0.05) 0px, rgba(63,122,93,0.05) 2px, transparent 2px, transparent 38px),
      repeating-linear-gradient(25deg, rgba(232,179,61,0.03) 0px, rgba(232,179,61,0.03) 1px, transparent 1px, transparent 50px);
    pointer-events:none;
    z-index:0;
  }

  .stage{position:relative; z-index:1; width:100%; max-width:560px; display:flex; flex-direction:column; align-items:center;}

  /* HEADER */
  .brand{
    display:flex; align-items:center; gap:10px;
    margin-bottom:6px;
  }
  .brand .paw{font-size:30px; filter:drop-shadow(0 2px 6px rgba(0,0,0,0.5));}
  .brand h1{
    font-family:'Cinzel Decorative', serif;
    font-weight:900;
    font-size:30px;
    letter-spacing:1px;
    margin:0;
    background:linear-gradient(180deg, var(--gold-bright), var(--gold) 60%, var(--gold-dim));
    -webkit-background-clip:text;
    background-clip:text;
    color:transparent;
    text-shadow:0 0 18px rgba(232,179,61,0.25);
  }
  .tagline{
    font-size:11.5px;
    letter-spacing:3px;
    text-transform:uppercase;
    color:var(--dim);
    margin-bottom:22px;
  }

  /* CABINET */
  .cabinet{
    position:relative;
    background:
      linear-gradient(160deg, var(--bg-panel-2), var(--bg-panel) 60%);
    border:3px solid var(--gold-dim);
    border-radius:26px;
    padding:20px 18px 22px;
    width:100%;
    box-shadow: var(--shadow-deep), inset 0 0 40px rgba(0,0,0,0.4);
  }
  .cabinet::before, .cabinet::after{
    content:"";
    position:absolute;
    width:26px; height:26px;
    border:2px solid var(--gold);
    border-radius:50%;
    box-shadow:0 0 10px rgba(232,179,61,0.5), inset 0 0 6px rgba(255,216,116,0.6);
    background:radial-gradient(circle at 35% 30%, var(--gold-bright), var(--gold-dim));
  }
  .cabinet::before{top:-13px; left:-13px;}
  .cabinet::after{top:-13px; right:-13px;}
  .corner-bl, .corner-br{
    position:absolute;
    width:26px; height:26px;
    border:2px solid var(--gold);
    border-radius:50%;
    box-shadow:0 0 10px rgba(232,179,61,0.5), inset 0 0 6px rgba(255,216,116,0.6);
    background:radial-gradient(circle at 35% 30%, var(--gold-bright), var(--gold-dim));
  }
  .corner-bl{bottom:-13px; left:-13px;}
  .corner-br{bottom:-13px; right:-13px;}

  .cabinet-top{
    display:flex; justify-content:space-between; align-items:center;
    margin-bottom:14px;
  }
  .status-pill{
    display:flex; align-items:center; gap:7px;
    background:rgba(0,0,0,0.3);
    border:1px solid rgba(232,179,61,0.3);
    border-radius:999px;
    padding:7px 14px;
    font-family:'JetBrains Mono', monospace;
    font-weight:700;
    font-size:14px;
  }
  .status-pill.balance{color:var(--gold-bright);}
  .status-pill.win{color:#7be0a0; min-width:96px; justify-content:center;}
  .status-pill .ico{font-size:14px;}

  .paytable-btn{
    background:none; border:1px solid rgba(232,179,61,0.4); color:var(--gold);
    width:30px; height:30px; border-radius:50%; cursor:pointer; font-size:13px;
    display:flex; align-items:center; justify-content:center;
    transition:0.15s;
  }
  .paytable-btn:hover{background:rgba(232,179,61,0.15);}

  /* REELS */
  .reels-frame{
    position:relative;
    background:#06100a;
    border-radius:16px;
    padding:12px;
    border:2px solid rgba(232,179,61,0.35);
    box-shadow:inset 0 0 30px rgba(0,0,0,0.7), 0 0 0 6px rgba(0,0,0,0.25);
  }
  .reels-frame::before{
    content:"";
    position:absolute; inset:0;
    border-radius:16px;
    background:linear-gradient(180deg, rgba(0,0,0,0.5), transparent 18%, transparent 82%, rgba(0,0,0,0.55));
    pointer-events:none;
  }
  .grid{
    display:grid;
    grid-template-columns:repeat(3, 1fr);
    grid-template-rows:repeat(3, 1fr);
    gap:8px;
  }
  .cell{
    aspect-ratio:1/1;
    background:linear-gradient(160deg, #163322, #0d2217);
    border:1px solid rgba(232,179,61,0.18);
    border-radius:10px;
    display:flex;
    align-items:center;
    justify-content:center;
    font-size:38px;
    user-select:none;
    position:relative;
    transition:transform 0.15s, box-shadow 0.3s, border-color 0.3s;
  }
  .cell.spinning{
    animation:blur-cycle 0.09s linear infinite;
  }
  @keyframes blur-cycle{
    0%{filter:blur(0px);}
    50%{filter:blur(1.5px);}
    100%{filter:blur(0px);}
  }
  .cell.win-cell{
    border-color:var(--gold-bright);
    background:linear-gradient(160deg, #2a4a1f, #1b3826);
    box-shadow:0 0 18px rgba(255,216,116,0.55), inset 0 0 14px rgba(255,216,116,0.25);
    animation:pop 0.5s ease infinite;
  }
  @keyframes pop{
    0%,100%{transform:scale(1);}
    50%{transform:scale(1.08);}
  }

  /* CONTROLS */
  .controls{
    display:flex;
    flex-wrap:wrap;
    align-items:center;
    gap:10px;
    margin-top:16px;
  }
  .bet-box{
    display:flex; align-items:center;
    background:rgba(0,0,0,0.3);
    border:1px solid rgba(232,179,61,0.3);
    border-radius:14px;
    overflow:hidden;
  }
  .bet-box button{
    background:none; border:none; color:var(--gold);
    font-size:18px; font-weight:700; width:34px; height:38px; cursor:pointer;
    transition:0.15s;
  }
  .bet-box button:hover{background:rgba(232,179,61,0.15);}
  .bet-box .bet-amt{
    font-family:'JetBrains Mono', monospace;
    font-weight:700;
    font-size:15px;
    padding:0 10px;
    min-width:64px;
    text-align:center;
  }

  .spin-btn{
    flex:1;
    min-width:140px;
    position:relative;
    background:radial-gradient(circle at 35% 30%, var(--gold-bright), var(--gold) 55%, var(--gold-dim));
    border:none;
    border-radius:16px;
    height:54px;
    font-family:'Poppins', sans-serif;
    font-weight:800;
    font-size:16px;
    letter-spacing:1px;
    color:#241405;
    cursor:pointer;
    box-shadow:0 6px 0 var(--gold-dim), 0 10px 18px rgba(0,0,0,0.4);
    transition:transform 0.08s, box-shadow 0.08s;
    text-transform:uppercase;
    display:flex; align-items:center; justify-content:center; gap:8px;
  }
  .spin-btn:active{
    transform:translateY(4px);
    box-shadow:0 2px 0 var(--gold-dim), 0 4px 10px rgba(0,0,0,0.4);
  }
  .spin-btn:disabled{
    filter:grayscale(0.4) brightness(0.8);
    cursor:not-allowed;
  }
  .spin-btn .paw-icon{font-size:20px;}

  .auto-btn{
    background:rgba(0,0,0,0.3);
    border:1px solid rgba(232,179,61,0.3);
    color:var(--dim);
    border-radius:14px;
    height:54px;
    width:54px;
    cursor:pointer;
    font-size:18px;
    transition:0.15s;
  }
  .auto-btn.active{
    background:rgba(179,38,42,0.25);
    border-color:var(--red-bright);
    color:var(--red-bright);
    box-shadow:0 0 12px rgba(230,61,61,0.4);
  }

  .bet-presets{
    display:flex; gap:6px; margin-top:10px; flex-wrap:wrap; justify-content:center;
  }
  .bet-presets button{
    background:rgba(0,0,0,0.25);
    border:1px solid rgba(159,182,166,0.25);
    color:var(--dim);
    font-family:'JetBrains Mono', monospace;
    font-size:12px;
    padding:6px 10px;
    border-radius:8px;
    cursor:pointer;
    transition:0.15s;
  }
  .bet-presets button.selected{
    border-color:var(--gold);
    color:var(--gold-bright);
    background:rgba(232,179,61,0.12);
  }

  /* FOOTER NOTE */
  .footnote{
    margin-top:22px;
    text-align:center;
    font-size:11px;
    line-height:1.6;
    color:var(--dim);
    max-width:420px;
  }
  .footnote b{color:var(--gold);}

  /* WIN TOAST (notificação brilhante para qualquer vitória) */
  .win-toast{
    position:fixed;
    top:26px;
    left:50%;
    transform:translate(-50%, -140%);
    z-index:70;
    display:flex;
    align-items:center;
    gap:10px;
    padding:13px 26px;
    border-radius:999px;
    background:linear-gradient(135deg, var(--gold-bright), var(--gold) 50%, var(--gold-dim));
    color:#241405;
    font-family:'Poppins', sans-serif;
    font-weight:700;
    font-size:15px;
    box-shadow:0 0 0 2px rgba(255,255,255,0.25) inset, 0 10px 30px rgba(232,179,61,0.55), 0 0 40px rgba(255,216,116,0.5);
    overflow:hidden;
    opacity:0;
    pointer-events:none;
    transition:transform 0.45s cubic-bezier(.34,1.56,.64,1), opacity 0.3s;
  }
  .win-toast.show{
    transform:translate(-50%, 0%);
    opacity:1;
    animation:toast-glow 1.1s ease-in-out infinite;
  }
  @keyframes toast-glow{
    0%,100%{ box-shadow:0 0 0 2px rgba(255,255,255,0.25) inset, 0 10px 30px rgba(232,179,61,0.55), 0 0 40px rgba(255,216,116,0.5); }
    50%{ box-shadow:0 0 0 2px rgba(255,255,255,0.4) inset, 0 10px 36px rgba(232,179,61,0.8), 0 0 60px rgba(255,216,116,0.85); }
  }
  .win-toast .toast-icon{font-size:20px; filter:drop-shadow(0 1px 3px rgba(0,0,0,0.3));}
  .win-toast .toast-text b{font-family:'JetBrains Mono', monospace; font-size:16px;}
  .win-toast .toast-shine{
    position:absolute;
    top:0; left:-60%;
    width:40%; height:100%;
    background:linear-gradient(115deg, transparent, rgba(255,255,255,0.75), transparent);
    transform:skewX(-20deg);
  }
  .win-toast.show .toast-shine{
    animation:shine-sweep 1.3s ease-in-out infinite;
  }
  @keyframes shine-sweep{
    0%{ left:-60%; }
    100%{ left:130%; }
  }

  /* WIN OVERLAY */
  .win-overlay{
    position:fixed; inset:0;
    display:flex; align-items:center; justify-content:center;
    background:rgba(5,10,7,0.55);
    backdrop-filter:blur(2px);
    z-index:50;
    opacity:0;
    pointer-events:none;
    transition:opacity 0.25s;
  }
  .win-overlay.show{opacity:1; pointer-events:all;}
  .win-card{
    background:linear-gradient(160deg, var(--bg-panel-2), var(--bg-panel));
    border:2px solid var(--gold);
    border-radius:22px;
    padding:30px 40px;
    text-align:center;
    box-shadow:0 0 60px rgba(232,179,61,0.4), var(--shadow-deep);
    transform:scale(0.8);
    transition:transform 0.3s cubic-bezier(.34,1.56,.64,1);
  }
  .win-overlay.show .win-card{transform:scale(1);}
  .win-card .tiger{font-size:54px; display:block; margin-bottom:6px;}
  .win-card h2{
    font-family:'Cinzel Decorative', serif;
    font-size:26px;
    margin:0 0 8px;
    color:var(--gold-bright);
    text-shadow:0 0 20px rgba(255,216,116,0.6);
  }
  .win-card .amount{
    font-family:'JetBrains Mono', monospace;
    font-size:30px;
    font-weight:700;
    color:#7be0a0;
    margin-bottom:14px;
  }
  .win-card button{
    background:var(--gold);
    border:none;
    color:#241405;
    font-weight:700;
    padding:9px 22px;
    border-radius:10px;
    cursor:pointer;
    font-family:'Poppins',sans-serif;
  }

  /* PAYTABLE MODAL */
  .modal-overlay{
    position:fixed; inset:0;
    background:rgba(5,10,7,0.65);
    display:flex; align-items:center; justify-content:center;
    z-index:60; opacity:0; pointer-events:none; transition:opacity 0.2s;
    padding:20px;
  }
  .modal-overlay.show{opacity:1; pointer-events:all;}
  .modal-box{
    background:linear-gradient(160deg, var(--bg-panel-2), var(--bg-panel));
    border:2px solid var(--gold-dim);
    border-radius:18px;
    padding:22px 24px;
    max-width:360px;
    width:100%;
    box-shadow:var(--shadow-deep);
  }
  .modal-box h3{
    font-family:'Cinzel Decorative', serif;
    color:var(--gold-bright);
    margin-top:0;
    font-size:18px;
    text-align:center;
  }
  .pay-row{
    display:flex; align-items:center; justify-content:space-between;
    padding:7px 4px;
    border-bottom:1px dashed rgba(159,182,166,0.2);
    font-size:13.5px;
  }
  .pay-row .sym{font-size:22px; width:34px;}
  .pay-row .desc{flex:1; color:var(--dim); padding:0 8px;}
  .pay-row .mult{font-family:'JetBrains Mono', monospace; color:var(--gold);}
  .modal-box .close-modal{
    display:block; margin:16px auto 0;
    background:none; border:1px solid var(--gold-dim); color:var(--gold);
    padding:7px 18px; border-radius:8px; cursor:pointer;
  }

  /* particles for jackpot */
  .particle{
    position:fixed;
    top:-20px;
    font-size:20px;
    pointer-events:none;
    z-index:55;
    animation:fall linear forwards;
  }
  @keyframes fall{
    to{ transform:translateY(110vh) rotate(360deg); opacity:0.2;}
  }

  @media (prefers-reduced-motion: reduce){
    .cell.spinning, .cell.win-cell, .win-overlay .win-card{animation:none !important; transition:none !important;}
  }

  /* ===== RESPONSIVO MOBILE ===== */
  @media (max-width: 480px){
    body{padding:18px 10px calc(50px + env(safe-area-inset-bottom));}
    .brand h1{font-size:24px;}
    .brand .paw{font-size:24px;}
    .tagline{font-size:10px; letter-spacing:2px; margin-bottom:16px;}

    .cabinet{padding:16px 12px 18px; border-radius:20px;}
    .cabinet-top{margin-bottom:10px;}
    .status-pill{font-size:12.5px; padding:6px 10px; gap:5px;}
    .status-pill.win{min-width:78px;}
    .paytable-btn{width:26px; height:26px; font-size:12px;}

    .reels-frame{padding:8px;}
    .grid{gap:6px;}
    .cell{font-size:28px; border-radius:8px;}

    .controls{gap:8px; margin-top:14px;}
    .spin-btn{min-width:0; flex:1 1 100%; order:3; height:50px; font-size:14.5px;}
    .bet-box{order:1; flex:1 1 calc(50% - 4px);}
    .bet-box button{width:30px; height:36px; font-size:16px;}
    .bet-box .bet-amt{font-size:14px; min-width:0; flex:1;}
    .auto-btn{order:2; flex:1 1 calc(50% - 4px); width:auto; height:50px;}

    .bet-presets{margin-top:8px;}
    .bet-presets button{font-size:11px; padding:5px 9px;}

    .footnote{font-size:10.5px; margin-top:16px;}

    .win-toast{
      top:calc(16px + env(safe-area-inset-top));
      padding:10px 18px;
      font-size:13px;
      max-width:92vw;
      gap:8px;
    }
    .win-toast .toast-icon{font-size:17px;}
    .win-toast .toast-text b{font-size:14px;}

    .win-card{padding:24px 22px; border-radius:18px; max-width:88vw;}
    .win-card .tiger{font-size:42px;}
    .win-card h2{font-size:20px;}
    .win-card .amount{font-size:24px;}

    .modal-box{padding:18px 18px; max-width:92vw;}
    .modal-box h3{font-size:16px;}
    .pay-row{font-size:12.5px;}
    .pay-row .sym{font-size:19px; width:28px;}
  }

  @media (max-width: 360px){
    .cell{font-size:24px;}
    .brand h1{font-size:20px;}
    .status-pill.win{min-width:64px;}
    .status-pill{font-size:11.5px;}
  }

  @media (max-height: 700px) and (max-width: 480px){
    body{padding-top:calc(14px + env(safe-area-inset-top));}
    .tagline{margin-bottom:10px;}
    .footnote{margin-top:10px;}
  }
</style>
</head>
<body>

<div class="stage">
  <div class="brand">
    <span class="paw">🐾</span>
    <h1>Tigrinho da Sorte</h1>
    <span class="paw">🐾</span>
  </div>
  <div class="tagline">Slot da Selva — Crédito Virtual</div>

  <div class="cabinet">
    <span class="corner-bl"></span>
    <span class="corner-br"></span>

    <div class="cabinet-top">
      <div class="status-pill balance"><span class="ico">🪙</span><span id="balance">1000</span></div>
      <button class="paytable-btn" id="openPaytable" title="Tabela de prêmios">?</button>
      <div class="status-pill win"><span class="ico">✨</span><span id="lastWin">+0</span></div>
    </div>

    <div class="reels-frame">
      <div class="grid" id="grid"></div>
    </div>

    <div class="controls">
      <div class="bet-box">
        <button id="betMinus">−</button>
        <div class="bet-amt"><span id="betAmt">25</span></div>
        <button id="betPlus">+</button>
      </div>
      <button class="spin-btn" id="spinBtn">
        <span class="paw-icon">🐯</span> Girar
      </button>
      <button class="auto-btn" id="autoBtn" title="Auto-giro">🔁</button>
    </div>

    <div class="bet-presets" id="betPresets"></div>
  </div>

  <p class="footnote">
    <b>Apenas entretenimento.</b> Este jogo usa moedas fictícias, sem qualquer valor real, e não envolve apostas com dinheiro de verdade. Jogue com moderação.
  </p>
</div>

<div class="win-toast" id="winToast">
  <span class="toast-icon">🪙</span>
  <span class="toast-text">Você ganhou <b id="toastAmount">0</b> moedas!</span>
  <span class="toast-shine"></span>
</div>

<div class="win-overlay" id="winOverlay">
  <div class="win-card">
    <span class="tiger" id="winEmoji">🐯</span>
    <h2 id="winTitle">RUGIDO DA FORTUNA!</h2>
    <div class="amount" id="winAmount">+0 🪙</div>
    <button id="closeWin">Continuar</button>
  </div>
</div>

<div class="modal-overlay" id="paytableModal">
  <div class="modal-box">
    <h3>Tabela de Prêmios</h3>
    <div id="payRows"></div>
    <p style="font-size:11.5px;color:var(--dim);text-align:center;margin:10px 0 0;">
      3 símbolos iguais em qualquer das 8 linhas (horizontais, verticais ou diagonais) pagam o multiplicador sobre a aposta. O 🐯 é selvagem e substitui qualquer símbolo.
    </p>
    <button class="close-modal" id="closePaytable">Fechar</button>
  </div>
</div>

<script>
(function(){
  const SYMBOLS = [
    { id:'clover', icon:'🍀', weight:30, mult:2,  name:'Trevo' },
    { id:'bell',   icon:'🔔', weight:25, mult:3,  name:'Sino' },
    { id:'envelope',icon:'🧧', weight:20, mult:5,  name:'Envelope da Sorte' },
    { id:'coin',   icon:'🪙', weight:15, mult:8,  name:'Moeda de Ouro' },
    { id:'gem',    icon:'💎', weight:7,  mult:15, name:'Gema' },
    { id:'tiger',  icon:'🐯', weight:3,  mult:50, name:'Tigrinho (Selvagem)', wild:true },
  ];

  const LINES = [
    [0,1,2],[3,4,5],[6,7,8],       // linhas
    [0,3,6],[1,4,7],[2,5,8],       // colunas
    [0,4,8],[2,4,6]                // diagonais
  ];

  const BET_PRESETS = [10,25,50,100,250];

  let balance = 1000;
  let betIndex = 1; // 25
  let spinning = false;
  let autoPlay = false;
  let autoTimer = null;
  let board = new Array(9).fill(SYMBOLS[0]);

  const $ = (id) => document.getElementById(id);
  const gridEl = $('grid');
  const balanceEl = $('balance');
  const lastWinEl = $('lastWin');
  const betAmtEl = $('betAmt');
  const betPresetsEl = $('betPresets');
  const spinBtn = $('spinBtn');
  const autoBtn = $('autoBtn');

  function weightedRandom(){
    const total = SYMBOLS.reduce((s,sym)=>s+sym.weight,0);
    let r = Math.random()*total;
    for(const sym of SYMBOLS){
      if(r < sym.weight) return sym;
      r -= sym.weight;
    }
    return SYMBOLS[0];
  }

  function buildGrid(){
    gridEl.innerHTML = '';
    for(let i=0;i<9;i++){
      const cell = document.createElement('div');
      cell.className = 'cell';
      cell.dataset.idx = i;
      cell.textContent = board[i].icon;
      gridEl.appendChild(cell);
    }
  }

  function buildPresets(){
    betPresetsEl.innerHTML = '';
    BET_PRESETS.forEach((val, idx)=>{
      const b = document.createElement('button');
      b.textContent = val;
      if(idx === betIndex) b.classList.add('selected');
      b.addEventListener('click', ()=>{
        if(spinning) return;
        betIndex = idx;
        renderBet();
      });
      betPresetsEl.appendChild(b);
    });
  }

  function renderBet(){
    betAmtEl.textContent = BET_PRESETS[betIndex];
    [...betPresetsEl.children].forEach((b,idx)=>b.classList.toggle('selected', idx===betIndex));
  }

  function renderBalance(){
    balanceEl.textContent = balance;
  }

  function setSpinningUI(isSpinning){
    spinning = isSpinning;
    spinBtn.disabled = isSpinning;
    spinBtn.innerHTML = isSpinning ? '<span class="paw-icon">🐯</span> Girando...' : '<span class="paw-icon">🐯</span> Girar';
  }

  function evaluateBoard(finalBoard){
    let totalMult = 0;
    let winningCells = new Set();
    let bestSymbol = null;

    LINES.forEach(line=>{
      const symsInLine = line.map(i=>finalBoard[i]);
      const nonWild = symsInLine.find(s=>!s.wild);
      const allMatchOrWild = symsInLine.every(s=> s.wild || (nonWild ? s.id===nonWild.id : true));
      const allWild = symsInLine.every(s=>s.wild);

      if(allWild){
        totalMult += SYMBOLS.find(s=>s.wild).mult;
        line.forEach(i=>winningCells.add(i));
        bestSymbol = SYMBOLS.find(s=>s.wild);
      } else if(nonWild && allMatchOrWild){
        totalMult += nonWild.mult;
        line.forEach(i=>winningCells.add(i));
        if(!bestSymbol || nonWild.mult > bestSymbol.mult) bestSymbol = nonWild;
      }
    });

    return { totalMult, winningCells, bestSymbol };
  }

  function spin(){
    if(spinning) return;
    const bet = BET_PRESETS[betIndex];
    if(balance < bet){
      flashInsufficient();
      return;
    }

    balance -= bet;
    renderBalance();
    lastWinEl.textContent = '+0';
    setSpinningUI(true);

    const cells = [...gridEl.children];
    const finalBoard = Array.from({length:9}, ()=>weightedRandom());

    cells.forEach(c=>{
      c.classList.add('spinning');
      c.classList.remove('win-cell');
    });

    // spin cada coluna com parada escalonada (efeito clássico)
    const columns = [[0,3,6],[1,4,7],[2,5,8]];
    const cycleIntervals = [];

    columns.forEach((colIndices, colIdx)=>{
      const interval = setInterval(()=>{
        colIndices.forEach(i=>{
          cells[i].textContent = weightedRandom().icon;
        });
      }, 70);
      cycleIntervals.push(interval);

      setTimeout(()=>{
        clearInterval(interval);
        colIndices.forEach(i=>{
          cells[i].textContent = finalBoard[i].icon;
          cells[i].classList.remove('spinning');
        });

        if(colIdx === columns.length - 1){
          finishSpin(finalBoard, bet, cells);
        }
      }, 600 + colIdx*350);
    });
  }

  function finishSpin(finalBoard, bet, cells){
    board = finalBoard;
    const { totalMult, winningCells, bestSymbol } = evaluateBoard(finalBoard);
    const winAmount = totalMult * bet;

    if(winAmount > 0){
      balance += winAmount;
      lastWinEl.textContent = '+'+winAmount;
      winningCells.forEach(i=>cells[i].classList.add('win-cell'));

      showWinToast(winAmount);

      if(winAmount >= bet*20){
        launchParticles();
        showWinOverlay(bestSymbol, winAmount, true);
      } else if(winAmount >= bet*8){
        showWinOverlay(bestSymbol, winAmount, false);
      }
    }

    renderBalance();
    setSpinningUI(false);

    if(balance <= 0){
      setTimeout(()=>{
        balance = 1000;
        renderBalance();
        alert('Suas moedas acabaram! Recarregamos 1000 moedas virtuais para você continuar jogando por diversão.');
      }, 400);
    }

    if(autoPlay){
      autoTimer = setTimeout(()=>{
        if(autoPlay && balance >= BET_PRESETS[betIndex]) spin();
        else stopAuto();
      }, 1100);
    }
  }

  let toastTimer = null;
  function showWinToast(amount){
    const toast = $('winToast');
    $('toastAmount').textContent = amount;
    toast.classList.add('show');
    if(toastTimer) clearTimeout(toastTimer);
    toastTimer = setTimeout(()=>{
      toast.classList.remove('show');
    }, 1800);
  }

  function showWinOverlay(symbol, amount, jackpot){
    const overlay = $('winOverlay');
    $('winEmoji').textContent = jackpot ? '🐯🔥' : (symbol ? symbol.icon : '✨');
    $('winTitle').textContent = jackpot ? 'RUGIDO DA FORTUNA!' : 'BOA SORTE!';
    $('winAmount').textContent = '+'+amount+' 🪙';
    overlay.classList.add('show');
  }

  function launchParticles(){
    const icons = ['🐯','🪙','✨','🧧'];
    for(let i=0;i<24;i++){
      const p = document.createElement('div');
      p.className = 'particle';
      p.textContent = icons[Math.floor(Math.random()*icons.length)];
      p.style.left = Math.random()*100+'vw';
      p.style.animationDuration = (1.6 + Math.random()*1.4)+'s';
      p.style.fontSize = (16 + Math.random()*16)+'px';
      document.body.appendChild(p);
      setTimeout(()=>p.remove(), 3200);
    }
  }

  function flashInsufficient(){
    spinBtn.style.boxShadow = '0 0 0 3px var(--red-bright), 0 6px 0 var(--gold-dim)';
    setTimeout(()=>{ spinBtn.style.boxShadow = ''; }, 350);
  }

  function stopAuto(){
    autoPlay = false;
    autoBtn.classList.remove('active');
    if(autoTimer) clearTimeout(autoTimer);
  }

  function buildPaytable(){
    const container = $('payRows');
    container.innerHTML = '';
    [...SYMBOLS].sort((a,b)=>b.mult-a.mult).forEach(sym=>{
      const row = document.createElement('div');
      row.className = 'pay-row';
      row.innerHTML = `<span class="sym">${sym.icon}</span><span class="desc">${sym.name}</span><span class="mult">x${sym.mult}</span>`;
      container.appendChild(row);
    });
  }

  // EVENTS
  spinBtn.addEventListener('click', spin);

  autoBtn.addEventListener('click', ()=>{
    autoPlay = !autoPlay;
    autoBtn.classList.toggle('active', autoPlay);
    if(autoPlay && !spinning) spin();
    if(!autoPlay && autoTimer) clearTimeout(autoTimer);
  });

  $('betMinus').addEventListener('click', ()=>{
    if(spinning) return;
    betIndex = Math.max(0, betIndex-1);
    renderBet();
  });
  $('betPlus').addEventListener('click', ()=>{
    if(spinning) return;
    betIndex = Math.min(BET_PRESETS.length-1, betIndex+1);
    renderBet();
  });

  $('closeWin').addEventListener('click', ()=> $('winOverlay').classList.remove('show'));
  $('openPaytable').addEventListener('click', ()=> $('paytableModal').classList.add('show'));
  $('closePaytable').addEventListener('click', ()=> $('paytableModal').classList.remove('show'));

  // INIT
  buildGrid();
  buildPresets();
  renderBet();
  renderBalance();
  buildPaytable();
})();
</script>
</body>
</html>
